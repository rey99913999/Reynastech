name: Actions smoke test

on:
  push:
    branches:
      - main

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Verify runner execution
        run: echo "GitHub Actions runner is executing workflows."
